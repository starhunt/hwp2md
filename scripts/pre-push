#!/bin/bash
# Pre-push hook for hwp2markdown
# Runs e2e tests before allowing push

set -e

echo "Running pre-push checks..."

# Run e2e tests
echo "==> Running e2e tests..."
go test -v -race ./tests/...

echo "All pre-push checks passed!"
